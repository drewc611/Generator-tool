// A script written for browsers that are gone.
var db = openDatabase("shop", "1.0", "orders", 2 * 1024 * 1024);
document.addEventListener("DOMNodeInserted", refresh, false);
if (document.all) { document.all.status.innerText = "ready"; }
function onKey(e) { var k = e.keyCode || e.which; return k; }
var q = escape(location.search);
var year = new Date().getYear();
if (navigator.userAgent.indexOf("MSIE") > -1) { el.attachEvent("onclick", go); }
window.onunload = save;
var xhr = new XMLHttpRequest(); xhr.open("GET", "/api/orders", false); xhr.send();
var raf = window.webkitRequestAnimationFrame;
var s = title.substr(0, 3);
