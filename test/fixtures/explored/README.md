# fixtures/explored

What `input-explore` wrote after driving `example/blackbox-app`. The style
samples are trimmed; everything else is exactly what came back.

It exists so the inference in `dsp-behavior` and `dsp-improve` runs in CI on a
machine with no browser, against a real recording rather than a hand written one.
