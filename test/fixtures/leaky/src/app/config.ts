// Placeholder values from public documentation. They authenticate nothing and
// exist so the policy engine has something to catch.
export const config = {
  region: 'us-east-1',
  awsKey: 'AKIAIOSFODNN7EXAMPLE',
  client_secret: 'not-a-real-secret-value'
};
