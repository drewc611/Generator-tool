# example/nosource

Deliberately empty. It is the source directory for a run against a system whose
source does not exist, so the source readers find nothing and the model comes
entirely from `input-explore` or from a recording it left behind.
